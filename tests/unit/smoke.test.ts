import { describe, expect, it } from "vitest";

// Placeholder that proves the Vitest layer runs in CI. Delete once real unit tests exist.
describe("vitest", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
